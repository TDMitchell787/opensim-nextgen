[package]
name = "glc-core"
version = "2.9.0"
edition = "2021"
license = "GPL-3.0"
description = "Core 3D types, mesh, material, and scene structures for GLC Player"

[dependencies]
glam = { version = "0.29", features = ["serde", "bytemuck"] }
serde = { version = "1", features = ["derive"] }
bytemuck = { version = "1", features = ["derive"] }
